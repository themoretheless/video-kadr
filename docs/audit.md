# Аудит: топ-200 «сделано плохо/неправильно»

Получено двумя проходами мульти-агентного ревью: 10 линз ищут проблемы по
реальному коду → каждую находку отдельный «скептик» проверяет, читая код, и
пытается **опровергнуть** (при неуверенности - отклонить). 104 сырых находки →
**118 подтверждено** верификацией, **14 опровергнуто** (ложные срабатывания, см.
конец). Первые 56 пунктов ниже - верифицированные дефекты/риски с привязкой к
коду; пункты 57-200 добивают список до архитектурного debt register и
синхронизированы с `architecture.md`/`recommendation.md`. Это заменяет первый
однопроходный вариант: две прошлые 🔴 («пустой ключ render_cache» и «fan-out
playerTime») оказались ложными и убраны.

Легенда: 🔴 баг/функц. дефект/потеря данных/гонка, бьёт по локальному
пользователю · 🟠 реальный риск · 🟡 долг/нит. Метка **⚠выставление** = проявляется
прежде всего при выставлении сервиса наружу (сейчас это локальный однопользова-
тельский MVP, README уже предупреждает «не выставляй как есть»).

Что НЕ баг (подтверждено верификацией, не переоценивать риск): инъекция команд
(нет shell, argv-массивы), path-traversal через `video_id` (скан директории по
stem, без join), `library.json` пишется атомарно (tmp+rename), полная сериализация
ключа кэша уже включает формат/расширение.

## Раунд 2 (1 июля 2026): пересборка после 5 P0-фиксов

Между первым проходом и сегодня в `main` влилось 5 коммитов, закрывающих
большинство P0 из этого документа (`a550a86` single-flight рендер-кэша,
`174e1a7` process-group kill, `596327b` атомарная отмена, `986b799` CORS/ServeDir,
`3b655c8` лимит на project payload) - подробности и статус каждого пункта P0-1…P0-8
в [recommendation.md](../recommendation.md). Второй раунд: 11 агентов читают
**текущий** код (не старые находки) - 5 верифицируют каждый из 5 коммитов построчно
и ищут регрессии в самом фиксе, 6 делают свежий проход по областям, которые фиксы
не трогали. 66 сырых находок → 23 подтверждены adversarial-верификацией
(скептик пытался опровергнуть каждую), 2 опровергнуты, 15 дополнительных
низкоприоритетных находок оставлены без отдельной верификации, 26 раз агенты
явно перепроверили заявленный фикс и подтвердили, что он корректен. Новые
пункты пронумерованы 201+ (нумерация 1-200 не переиспользуется - зафиксированные
пункты помечены ✅ на месте, а не удаляются, чтобы ссылки между документами не
протухали). Топ-50 актуальных проблем - сразу ниже; новые находки целиком -
в разделе «Раунд 2: новые находки»; что именно было перепроверено и
подтверждено рабочим - в «Раунд 2: перепроверено и подтверждено».

## Топ-50 актуальных проблем (1 июля 2026)

Статусы синхронизированы 11 июля 2026: список и нумерация сохранены как
аудиторское доказательство, актуальная очередь работ находится в
`recommendation.md`.

На дату снимка P0-фиксы в основном держали заявленное, но 2 из старых
«самых критичных» пунктов (№18 upload-семафор, №33 SSRF) оставались открытыми
почти в исходном виде, а фикс отмены (`596327b`) закрыл гонку по терминальным
статусам, но **не** закрыл соседнюю гонку по переходу в `Running` (см. №203).
Кроме того, впервые за всю историю аудита найден и подтверждён рабочим PoC
эксплуатируемый баг (№202, stored XSS через upload) - раньше upload-magic-bytes
шёл как гипотетический риск (№38/№182), теперь это воспроизведённый эксплойт.

🟠 высокий / 🟡 средний / ⚪ низкий - шкала этого раздела не совпадает 1:1 с
🔴/🟠/🟡 остального документа: после 5 фиксов в кодовой базе локального
однопользовательского MVP не осталось пунктов уровня «бьёт по пользователю
прямо сейчас без внешнего триггера», поэтому верхняя планка серьёзности здесь -
🟠 (эксплуатируемо удалённым сервером/второй вкладкой, не требует уже
скомпрометированной машины).

1. ✅ №201 - редирект `yt-dlp` на приватный адрес обходил SSRF-проверку; закрыто per-job egress-proxy в раунде 6.
2. ✅ №202 - upload принимал полиглот-файл и отдавал его как `text/html`; закрыто content-derived extension + headers в раунде 5.
3. ✅ №203 - отмена между `is_cancelled()` и записью `Running` перетиралась; закрыто атомарным `update_job_if_open` в раунде 5.
4. ✅ №18 - `upload_handler` миновал concurrency gate; закрыто отдельным upload pool и bounded probe в раунде 7.
5. 🟡 №24 - TTL-чистка сверяется только с `library`, не с активными/queued job (частично исправлено: file/cache/library теперь консистентны между собой, но не с job-очередью).
6. 🟡 №204 - `upsert_project` не атомарен, конкурентный автосейв из двух вкладок даёт дубликаты project-строк.
7. 🟡 №19/№20 - у ffmpeg/yt-dlp нет CPU/RAM/filesize-лимитов и no-progress watchdog (реконфирмировано).
8. 🟡 №205 - `render_locks` в `AppState` растёт неограниченно, записи никогда не удаляются (побочный эффект `a550a86`).
9. 🟡 №206 - в `174e1a7` путь «оба потока дали EOF раньше child» шлёт только SIGTERM без эскалации до SIGKILL.
10. 🟡 №207 - лимит `3b655c8` покрывает `video`/`edit`, но не `name` - можно записать ~2МБ в `projects.name`.
11. 🟡 №208 - `fade_out`/`fade_in` клэмпятся к длительности исходника, а не результата - при trim/segments/speed фейд может молча выпасть из графа фильтров.
12. 🟡 №26 - в SQLite нет миграций, `schema_version` пишется и не читается (реконфирмировано).
13. 🟡 №27 - таблица `jobs` не имеет ретеншна вообще (строки не удаляются никогда); `render_cache` чистится только реактивно (частично исправлено: `recover_jobs` теперь лимитирован).
14. 🟡 №30/№31 - `cache_put`/`library.add` всё ещё не атомарны, `library.add` глотает ошибку персиста (частично исправлено `def47d4`, остаточный путь открыт).
15. ⚪ №209 - `PRESET_KEYS` включает `format`/`codec`/`qualityTier` наравне с цветом - применение цветового пресета тихо меняет контейнер/качество экспорта (переформулировка №48 под текущий код).
16. 🟡 №210 - `RectOverlay` не снимает `window` pointermove/pointerup при размонтировании во время drag → `root.value!` бросает `TypeError`; путь воспроизведён (Ctrl+Z/сброс crop мышью во время драга).
17. ⚪ №211 - провал задачи (`finish_job` `Err`-ветка) не пишет `tracing::error!` - ни одной диагностики в логах на упавший job.
18. ⚪ №53 - `Dockerfile` тянет `yt-dlp` через `curl` без `-f` и без пина/чексуммы (реконфирмировано).
19. 🟡 №212 - CI не ставит `yt-dlp` - путь импорта не выполняется в CI ни разу (реконфирмировано, теперь с точной привязкой к `ci.yml:16-19`).
20. 🟡 №50 - нет теста на полный happy-path `edit_handler` (permit→drain→ffmpeg→cache→library); все текущие edit-тесты либо предзаряжают кэш, либо бьют по отсутствующему source.
21. ⚪ №50 - нет теста на отмену реально `Running` job с убийством процесса (все cancel-тесты останавливаются на `Pending`/`Queued`).
22. 🟡 №50 - `doImport`/`doExport`/`doUpload` не покрыты тестами, хотя `api`-мок в `store.test.ts` для этого уже есть.
23. ⚪ №213 - `cache_put` может тихо не сработать после успешного рендера A → воркер B перерендеривает вместо переиспользования готового файла.
24. ⚪ №214 - деградированный crop/censor-клэмп может дать нулевую/нечётную ширину на источнике 1px.
25. ⚪ №215 - `list_projects`/`load_jobs`/`load_recent_jobs` роняют весь список 500-й, если у одной строки испорчен JSON.
26. ⚪ №31 - `set_job` глотает ошибку `persist_job` при создании задачи (реконфирмировано).
27. ⚪ №216 - `ffmpeg`/`yt-dlp`/`ffprobe` резолвятся через голый `PATH`, не через фиксированный абсолютный путь.
28. ⚪ №41 - сырой stderr ffmpeg/yt-dlp (с локальными путями) всё ещё уходит в `job.error` клиенту (реконфирмировано).
29. ⚪ №77 - `cancelJob` на фронте игнорирует статус/тело ответа сервера (реконфирмировано).
30. 🟡 №217 - `TrimSlider` не снимает `window` pointermove/pointerup при размонтировании во время drag (тот же паттерн, что №210).
31. ⚪ №218 - `normalizeRect` маскирует `NaN` в `x/y` как `0` и в `w/h` как минимум вместо явной валидации - повреждённый прямоугольник тихо телепортируется в угол.
32. ⚪ №59 - нет `rust-toolchain.toml`/MSRV, сборка зависит от плавающего `stable` и в CI, и в Dockerfile (реконфирмировано).
33. ⚪ №50 - cap на 100 записей истории (`history.past.length > 100`) не покрыт тестом.
34. 🟡 №34 ⚠выставление - по-прежнему нет auth/ownership на `projects`-эндпоинтах (не новое, но перепроверено: `project_get`/`project_list`/`project_delete` всё ещё без владельца).
35. 🟡 №40 - ни один request-DTO не имеет `#[serde(deny_unknown_fields)]`, неизвестные поля тихо игнорируются.
36. ⚪ №42/№43 - три разные формы ошибок в projects API, часть DB-ошибок превращается в голый 500 без тела/лога.
37. ⚪ №44 - создание async-задачи (`import`/`edit`) отвечает `200`, а не `202 Accepted`.
38. 🟡 №56 - контракт правки по-прежнему трогает ~6 несинхронизированных мест на одно новое поле, ни одно не ловится компилятором.
39. ✅ №22 - частичный upload удаляется при multipart/I/O/timeout через единый receive boundary (закрыто в раунде 7).
40. 🟡 №21 - нет cap на filesize/duration импорта помимо `MAX_HEIGHT` (не тронуто фиксами, не перепроверялось этим раундом).
41. ⚪ №45 - `safeFetch` по-прежнему технически смешивает network-failure и 5xx в одно сообщение, но `import`/`edit`-хендлеры сейчас физически не могут вернуть 5xx - на практике не проявляется (понижено, было 🟡 в исходном аудите).
42. 🟠 №5 - graceful shutdown бросает in-flight воркеры и их дочерние процессы при остановке сервера (не тронуто фиксами).
43. 🟡 №28 - `library.json` и SQLite остаются двумя независимыми хранилищами без общего FK/id-контракта и могут расходиться при частичных сбоях.

**Честно про счёт: 43, не 50.** Это весь пул пунктов, которые в этом раунде
прошли либо adversarial-верификацию, либо явную повторную проверку кода (не
безосновательное «то же самое, наверное, всё ещё сломано»); хвостовые пункты
раздела K (57-200) - реальный, но архитектурный/фичевый долг, не «сделано
неправильно» в смысле этого списка, и туда просто не добавлено 7 находок ради
круглого числа. Полностью новые находки (201-218) с деталями и `file:line` - в разделе
«Раунд 2: новые находки» после раздела J. Что перепроверено и подтверждено
рабочим (закрывает старые пункты) - в разделе «Раунд 2: перепроверено».

## Самое критичное (чинить первым) - исходный раунд, для истории

№1 (гонка cancel↔finish: отменённая задача может стать Done), №2 (отмена/таймаут
импорта оставляет мусор на диске), №9 (segments молча игнорируются для
AV1/ProRes - функц. дефект), №10 (crop не валидируется против размеров источника →
обрыв ffmpeg + утечка пути), №18 (upload минует семафор - DoS), №24 (TTL-чистка
удаляет файлы без проверки ссылок → сироты), №33 (SSRF обходится через DNS yt-dlp).

*(Номера здесь исправлены 1 июля 2026: в исходном тексте была опечатка —
callout ссылался на №1/№2/№3/№4/№17/№23/№32, что не совпадало с реальной
нумерацией пунктов в разделах A-E ниже; сама подборка «самого критичного»
осталась той же семёркой, поменялись только ссылочные номера.)*

**Статус на 11 июля 2026:** ✅ №1, ✅ №2 закрыты и перепроверены (терминальные
переходы cancel/finish и очистка мусора импорта; соседняя гонка на переходе в
`Running` позже закрыта в раунде 5 - см. №203).
✅ №9, ✅ №10 закрыты и перепроверены. ✅ №18 и ✅ №22 закрыты в раунде 7
отдельным upload pool и единым cleanup boundary; ☐ №24 (только file/cache/library-консистентность, не active-job)
остаётся открытым. ✅ №33/№201 закрыты полностью в раунде 6 (initial validation,
redirect-hop и DNS rebinding).

## A. Корректность задач и гонки

1. 🔴 `handlers/mod.rs:382-409` + `427-459` - гонка `cancel_handler`↔`finish_job`: две неатомарные секции под разными захватами лока, `finish_job` (в отличие от cancel) НЕ проверяет `is_terminal()` перед записью → отменённая пользователем задача может перезаписаться в `Done`. → гейтить терминальный переход на `!is_terminal()`/токен отмены в одной критической секции. **✅ Исправлено (`596327b`, перепроверено 1 июля 2026): `cancel_open_job`/`update_job_if_open` под одним локом `jobs`.**
2. 🔴 `handlers/mod.rs:104-106, 439-454` - отмена/таймаут импорта не чистит частично скачанное (`<id>.*`, `.part`, `<id>.info.json`); edit-путь чистит (`:337`), import - нет; при `FILE_TTL_HOURS=0` мусор копится бесконечно. → подмести `sources/` по префиксу `vid` в ветках Cancelled и Err. **✅ Исправлено (по plan.md P0-A); соседняя гонка на переходе в `Running` не входила сюда и остаётся открытой - см. №203.**
3. 🔴 `handlers/mod.rs:268-291` + `382-409` - кэш-хит edit отменяем в негейченном окне → мгновенно готовый рендер репортится как `Cancelled`. → проверять статус/токен перед записью Done на кэш-хите. **✅ Исправлено (`596327b`): `finish_from_render_cache` пишет Done через `update_job_if_open`.**
4. 🟠 `tools/mod.rs:303, 346-349` - внук `ffmpeg`, порождённый `yt-dlp`, осиротевает при отмене/таймауте (убит только прямой ребёнок, нет process-group). → `process_group(0)` + kill negative pgid. **✅ Исправлено (`174e1a7`), перепроверено 1 июля 2026: `process_group(0)` + `kill(-pgid)` реально накрывает внука; остаточный гэп в не-cancel/timeout ветке - см. №206.**
5. 🟠 `main.rs:90-93` - graceful shutdown бросает in-flight воркеры и их дочерние процессы (HTTP закрыт, задачи нет). → `TaskTracker` + отмена воркеров. **☐ Не тронуто фиксами, не перепроверялось этим раундом - см. топ-50 №42.**
6. 🟡 `handlers/mod.rs:64-79, 296-311` - ожидание permit не отменяемо: отменённая «queued» задача всё равно держит очередь. → `select!` permit против `token.cancelled()`. **✅ Исправлено, перепроверено 1 июля 2026.**
7. 🟡 `handlers/mod.rs:66-69, 298-301` - ветка `acquire_owned() => Err` делает голый `return`: задача навсегда в non-terminal, течёт cancel-токен. → перевести в `Error`, `persist`, `clear_cancel`. **✅ Исправлено (recommendation.md P0-4).**
8. 🟡 `handlers/mod.rs:412-422` - progress-drain продолжает писать `progress` на уже отменённую/терминальную задачу. → игнорировать тики после терминала.

## B. Геометрия и сегменты (функциональная корректность правок)

9. 🔴 `tools/args.rs:234, 491-496` + `store.ts:213-226` - вырезание куска (segments) молча НЕ работает для AV1 и ProRes: concat-ветка не строится, экспортируется неразрезанное. → строить concat для всех видеоформатов или явно запрещать в UI. **✅ Исправлено (recommendation.md P0-6).**
10. 🔴 `handlers/mod.rs:331-333` + `tools/args.rs:75-79` - crop не валидируется против размеров источника (probe берётся только для duration); выход за кадр → ffmpeg падает → сырой stderr с путём уходит юзеру. → клампить crop к `width/height` из probe. **✅ Исправлено (recommendation.md P0-8); вырожденный случай источника 1px - см. №214.**
11. 🟠 `RectOverlay.vue:87-96` + `args.rs:75` - независимое округление `x` и `w` даёт `x+w > width` даже из штатного overlay. → клампить `x = min(x, W-w)` при эмите. **✅ Исправлено (recommendation.md P0-8: `x = min(x, W-w)` на фронте).**
12. 🟠 `tools/args.rs:491-496, 420-442` - сегменты не сортируются: переставленные сегменты перемешивают таймлайн. → сортировать по `start`. **✅ Исправлено, перепроверено 1 июля 2026: сегменты сортируются по `start`, пересечения отклоняются.**
13. 🟠 `tools/args.rs:407-431` - концы сегментов не клампятся к длительности (`end > duration` идёт сырым в `trim/atrim`). → клампить к `source_duration`. **✅ Исправлено (plan.md P0-A: сегменты/trim клампятся к `[0, probe.duration]`).**
14. 🟠 `tools/args.rs:493, 420-430` - отрицательный `start` принимается и эмитится как `trim=start=<negative>`. → отбраковывать/клампить `>= 0`. **✅ Исправлено (тот же P0-A клампинг).**
15. 🟠 `tools/args.rs:420-442` - перекрывающиеся сегменты дублируют кадры без детекции. → мержить/отклонять пересечения. **✅ Исправлено, перепроверено 1 июля 2026: пересечения отклоняются в `handlers/mod.rs:598-618`.**
16. 🟠 `tools/args.rs:208-215, 280-293` - `fps` без верхней границы идёт прямо в `-r` (а в gif - в `palettegen/paletteuse`) → взрыв числа кадров, CPU/mem DoS; крошечный `0.0001` обходит `>0.0` и даёт `-r 0.000`. → клампить fps в разумный диапазон. **✅ Исправлено (recommendation.md P0-8: backend валидирует `fps`/`scale`).**
17. 🟡 `tools/args.rs:96-98` + `model.rs:208-212` - `Scale` (i32) эмитится дословно: значимы только `-1/-2`, прочие отрицательные/нечётные ширины ломают энкод. → валидировать scale.

## C. Ресурсы и DoS

18. 🔴 `handlers/mod.rs:142-229` - `upload_handler` полностью миновал concurrency gate: неограниченная запись на диск + `ffprobe` на каждый запрос. → ограничить upload и probe. **✅ Закрыто в раунде 7:** отдельный fail-fast upload pool, bounded multipart receive с cleanup и 30-секундный `ffprobe` timeout.
19. 🟠 `tools/mod.rs:83-103, 256-290` - нет лимита CPU/RAM/threads/filesize на ffmpeg/yt-dlp; только счётный семафор → насыщение/OOM. → `-threads`, `nice`, лимиты в compose.
20. 🟠 `tools/mod.rs:295-352` - нет no-progress watchdog: зависший-но-живой процесс держит слот весь `JOB_TIMEOUT` (1800с). → watchdog по бездействию.
21. 🟠 `tools/mod.rs:73-103` - нет cap на размер/длительность импорта (`MAX_HEIGHT` ограничивает только высоту). → `--max-filesize`, лимит длительности.
22. ✅ `handlers/upload.rs` - частичный upload оставался на диске при обрыве по лимиту тела / ошибке multipart. **Закрыто в раунде 7:** pre-publish receive boundary удаляет `.upload` при parse/I/O/timeout до возврата ответа.
23. 🟡 `handlers/mod.rs:411-422` + `state.rs:73-77` - `update_job` на каждый progress-тик берёт глобальный `jobs` Mutex, общий со всеми чтениями. → throttle прогресса / отдельный канал.

## D. Данные и целостность

24. 🔴 `main.rs:103-135` - TTL-чистка удаляет файлы по mtime без проверки ссылок → сироты projects/jobs/cache; может удалить source под идущим/очередным рендером. → удалять только нессылаемые, не трогать активные.
25. 🟠 `db.rs:40-46` + `main.rs:103-134` + `library.rs` - `render_cache` не инвалидируется при удалении выходного файла → кэш-хит на удалённый файл = 404. → `cache_delete` при промахе файла и при `library.remove`. **✅ Исправлено (recommendation.md P0-5), перепроверено 1 июля 2026.**
26. 🟠 `db.rs:18-46` - нет миграций: `schema_version` пишется, но не читается; изменение схемы сломает существующую БД. → `PRAGMA user_version` + пошаговые миграции.
27. 🟠 `state.rs:92-109` + `db.rs:206-213` - нет ретеншна jobs/render_cache (растут вечно); `recover_jobs` грузит ВСЕ строки в память при старте. → TTL-очистка + лимит загрузки.
28. 🟠 `library.rs:90-137` + `db.rs:40-46` - два параллельных хранилища (`library.json` vs SQLite) без FK/общего id, дрейфуют независимо. → унифицировать в SQLite. **☐ Реконфирмировано 1 июля 2026, не тронуто фиксами - см. топ-50 №43.**
29. 🟠 `handlers/mod.rs:262-368` - нет single-flight: два идентичных параллельных edit оба мимо кэша, оба рендерят, гонка на `cache_put`/`library.add`, сирота-output. → in-flight карта по `cache_key`. **✅ Исправлено (`a550a86`), перепроверено 1 июля 2026: per-cache_key lock корректно сериализует конкурентов, включая error/cancel/panic держателя. Побочный эффект - неограниченный рост карты локов, см. №205.**
30. 🟡 `handlers/mod.rs:357-364` + `427-459` - `cache_put` и `library.add` не атомарны: краш между ними → кэш без библиотеки (или наоборот). → писать кэш после успешной регистрации.
31. 🟡 `state.rs:60-65` - `set_job` персистит синхронно, но **глотает ошибку**: задача живёт в памяти без durable-записи и пропадёт после рестарта. → на критический сбой persist возвращать ошибку.
32. 🟡 `db.rs:18-46, 135-143` - нет индексов на `jobs.updated_at`/`render_cache.created_at` (есть только `idx_projects_video_id`), хотя по ним идёт сортировка/ретеншн. → добавить индексы. **✅ Исправлено, перепроверено 1 июля 2026: `idx_jobs_updated_at`, `idx_render_cache_created_at` есть в схеме (но пока не используются ни в одном ретеншн-запросе - retention сам по себе всё ещё не реализован, см. №27).**

## E. Безопасность (в основном ⚠выставление)

33. 🟠 `tools/net.rs:10-47` + `tools/mod.rs:103` - SSRF обходился: host резолвил yt-dlp, а initial guard не контролировал фактический connect. → резолвить host и проверять все A/AAAA, запрет приватных редиректов. **✅ Закрыто полностью в раунде 6:** initial validation и per-request egress-proxy используют одну policy, DNS answer проверяется целиком, connect идёт к pinned `SocketAddr`; см. №201.**
34. 🔴⚠выставление `lib.rs:28-58` - нет аутентификации ни на одном эндпоинте. → middleware с токеном перед выставлением.
35. 🟠⚠выставление `lib.rs:54` - `ServeDir` отдаёт весь `storage`, включая `app.db` и `library.json`. → вынести БД из `storage`, отдавать за авторизацией. **✅ Исправлено (`986b799`), перепроверено 1 июля 2026: `/files` смонтирован раздельно на `sources/`/`outputs/`, `..`-traversal невозможен.**
36. 🟠⚠выставление `lib.rs:56` - `CorsLayer::permissive()` на мутирующих POST/DELETE. → ограничить origin/методы. **✅ Исправлено (`986b799`), перепроверено 1 июля 2026: явный allowlist, fail-closed при отсутствии `CORS_ALLOW_ORIGINS`.**
37. 🟡 `tools/net.rs:29-47` - список заблокированных диапазонов без CGNAT (`100.64/10`) и IPv4-mapped IPv6 (`::ffff:127.0.0.1`). → полный special-use allowlist. **✅ Исправлено, перепроверено 1 июля 2026: `validate_url` резолвит DNS и блокирует полный набор спец-диапазонов.**
38. 🟡 `handlers/mod.rs:149-229` - upload не проверяет тип/magic-байты до того, как файл доверен и отдаётся через `/files`. → проверка контейнера/magic.
39. 🟡 `handlers/projects.rs:14-44` - `project_upsert` хранит произвольный JSON-блоб дословно (нет схемы/лимита). → типизированный DTO + лимит размера. **◐ Наполовину исправлено (`3b655c8`), перепроверено 1 июля 2026: `video`/`edit` лимитированы 64KiB и проверка идёт до записи в БД корректно по байтам; поле `name` осталось без лимита - см. №207.**
40. 🟡 `model.rs:83-212` - нет `#[serde(deny_unknown_fields)]` ни на одном request-DTO; неизвестные поля молча игнорируются. → строгая десериализация.

## F. Ошибки и API-контракт

41. 🟠 `tools/mod.rs:131-146, 288` - сырой stderr ffmpeg/yt-dlp (с путями) уходит в `job.error` и показывается пользователю. → типизированные ошибки, безопасный текст наружу, техника в лог.
42. 🟡 `handlers/projects.rs:17,49,62,74,86` - три разные формы ошибки в одном API (`(StatusCode,String)` / plain-text / голый код). → общий `ApiError: IntoResponse`.
43. 🟡 `handlers/projects.rs:63-91` - `Err(_) => 500` глотает причину БД без тела и без лога. → единый маппинг + `tracing::error!`.
44. 🟡 `handlers/mod.rs:136, 262-265` - создание async-задачи отвечает `200`, а не `202 Accepted`. → `StatusCode::ACCEPTED`.
45. 🟡 `api.ts:24-28, 45-58` - фронт трактует любой `>=500` как «бэкенд недоступен», маскируя реальные 500 с телом; `cancelJob` (`:117-124`) игнорирует ответ. → различать сетевой сбой и HTTP 5xx. **⚪ Понижено 1 июля 2026: механизм подтверждён (по-прежнему смешивает network-failure и 5xx), но `import`/`edit`-хендлеры сейчас физически ни разу не возвращают 5xx, так что на практике не проявляется - латентный, не активный баг. `cancelJob` игнорирует ответ - подтверждено, остаётся отдельным нитом.**

## G. Frontend (структура/качество)

46. 🟠 `store.ts` (609 строк) + `EditPanel.vue:315-724` - god-модуль (один reactive, прямые мутации из компонентов) + god-компонент (8 доменов в одном файле). → разбить на core + features composables / секции-компоненты (см. recommendation.md P1-5/6).
47. 🟡 `store.ts:440-447, 598-609` - два `watch()` регистрируются как сайд-эффект импорта модуля (нельзя выключить/замокать; текут между тестами); они же дублируют deep-обход `state.edit`. → `initStore()`/composable, один объединённый watch.
48. 🟡 `store.ts:453-456, 514-531` - ~~`applyPreset` делает `Object.assign` без фильтра по `PRESET_KEYS`, плюс `loadPresets`/`applySnapshot`/`getProjectByVideo`/`job.result` приводят недоверенный JSON через `as` без runtime-валидации → устаревший/чужой пресет затирает геометрию, битый payload портит редактор.~~ **Переформулировано 1 июля 2026 по итогам adversarial-проверки: исходная формулировка устарела/неточна.** `applyPreset` **сейчас** фильтрует по `PRESET_KEYS` (не голый `Object.assign`), но сам `PRESET_KEYS` слишком широк - включает `format`/`codec`/`qualityTier` наравне с цветом, так что цветовой пресет тихо меняет контейнер экспорта - это реальный, но другой баг, см. №209. Часть про `job.result as VideoInfo` опровергнута: `job.result` - типизированный ответ **собственного** бэкенда, а не чужой JSON, и обрабатывается ровно как любой другой `api.*()`-результат в приложении. Часть про `loadPresets` (localStorage без runtime-валидации формы) - подтверждена, но с прослеженным impact «максимум один 400 от бэкенда или no-op merge», не «затирает геометрию» - понижено до ⚪, отдельно не нумеруется, входит в №209/бэклог низкоприоритетных нитов. → разделить `PRESET_KEYS` на look/export-группы (см. №209 fix).
49. 🟡 `store.ts:336-342` + `EditPanel.vue:442-454` + `RectOverlay.vue:55-59` - проглоченные `catch {}` без лога; нет a11y (`fieldset/legend`, `role=radio`/`aria-pressed`/`aria-invalid`); `root.value!` в drag-математике. → логировать, добавить семантику/ARIA, guard вместо `!`.

## H. Тесты

50. 🟠 `tests/api.rs` + `store.test.ts` - не покрыто: happy-path рендера через `edit_handler` (оркестрация семафор/drain/library/cache), баг persist-on-URL-error, undo/redo/history, `run_with_progress`(kill), `map_ytdlp_error`/`sanitize_ext`, контракт-снэпшот JSON-ответов, async-экшены стора (нет api-мока). → добавить интеграционный (skip-если-нет-ffmpeg) + юнит-тесты.

## I. Конфиг / сборка / Docker / CI

51. 🟠 `main.rs:84` - `BIND_ADDR` при ошибке парсинга тихо откатывается в `127.0.0.1` (контейнер недостижим, в логе ничего). → строгий парсинг, `bail!`.
52. 🟠 `main.rs:51-83` - все числовые env fail-soft (`.parse().ok().unwrap_or`): `MAX_CONCURRENT_JOBS=0`/опечатка молча → дефолт. → ошибка старта на невалидном значении; единый `Config` (env россыпью в ~7 местах).
53. 🟠 `Dockerfile:9-13` - `yt-dlp` без пиннинга (`releases/latest`) + ffmpeg из apt без версии → невоспроизводимо, парсинг прогресса может сломаться. → пиннинг + sha256.
54. 🟡⚠выставление `Dockerfile:8-20` - бэкенд от root, нет `USER`/`HEALTHCHECK`/лимитов (`compose`). → непривилегированный user, healthcheck, лимиты.
55. 🟡 `ci.yml:17-31` - CI ставит ffmpeg, не ставит yt-dlp → import-путь не покрыт в CI; README «Node 18+» vs CI/Docker Node 20; README не упоминает `RUST_LOG`; нет MSRV. → добавить yt-dlp, выровнять доки/MSRV.

## J. Доменная модель

56. 🟠 `model.rs:95-181` + `types.ts:22-60` + `store.ts` - контракт правок продублирован в ~6 несинхронизированных местах без компиляторной проверки (тихий дроп поля); `EditRequest` тащит тройную роль (wire DTO + домен + вход билдера); нет валидации enum формата/кодека (неизвестное молча → mp4/H.264). → один источник истины (генерация TS из Rust), типобезопасный маппер. **Реконфирмировано 1 июля 2026, не тронуто фиксами.**

## Раунд 2: новые находки (201-218, 1 июля 2026)

Найдено 11 агентами (5 читали построчно diff'ы 5 P0-коммитов в поисках регрессий,
6 делали свежий проход по коду вне зоны этих коммитов), каждая находка выше
🟡 отдельно прогнана через adversarial-скептика (см. методологию в начале
файла). Нумерация продолжает 1-200 без переиспользования: старые номера не
меняются, если пункт закрыт - он помечен ✅ на месте, а не удалён.

### Security / concurrency

201. 🟠 `tools/mod.rs:83-107` - редирект `yt-dlp` на приватный/link-local адрес после успешной `validate_url` полностью обходит SSRF-защиту. `validate_url` (net.rs) резолвит DNS исходного URL и блокирует приватные/loopback/link-local/CGNAT IP один раз, до старта импорта; `download_video` передаёт тот же raw URL в `Command::new("yt-dlp")` без прокси, `--resolve`-пиннинга или повторной проверки конечного адреса. `yt-dlp` сам резолвит DNS и следует HTTP-редиректам, поэтому сервер, прошедший первичную проверку (публичный IP с коротким TTL, либо просто отдающий 30x на `169.254.169.254`/`127.0.0.1`), уводит фактический запрос `yt-dlp` на приватный адрес - классический TOCTOU/DNS-rebinding/redirect SSRF. Уже был предвиден в recommendation.md как открытый пункт P0-3, здесь впервые заведён как отдельная пронумерованная находка с привязкой к коду. → пиннинг резолвленного IP в `yt-dlp` (`--resolve`/сокет-опции) либо собственный HTTP-клиент с повторной DNS+IP-валидацией на каждый hop перед передачей финального URL в `yt-dlp`. `tools/mod.rs:83-107`

**Статус №201 на 11 июля 2026: ✅ закрыто в раунде 6.** `tools/egress_proxy.rs` проверяет каждый redirect/CONNECT target и пинит DNS answer в `SocketAddr`; format selector принимает только HTTP(S) media. Redirect, rebinding и RTMP-only tests не допускают private TCP connect/non-proxy downloader.

202. 🟠 `handlers/mod.rs:145-251` - upload принимает полиглот-файл с любым расширением и отдаёт его с `Content-Type` по расширению - подтверждённый stored XSS. `sanitize_ext` фильтрует только алфанумерику и не имеет allow-list, единственная проверка контента - `ffprobe` через гейт `i.width > 0 || i.duration > 0.0`. Воспроизведено рабочим PoC: валидный заголовок `GIF89a` (1×1) с дописанным `<script>alert(document.domain)</script>` и именем `poc.html` проходит `ffprobe` (codec_name=gif) и сохраняется как `<uuid>.html`; `/files/sources/<uuid>.html` отдаётся `ServeDir` с `text/html`, переход по прямой ссылке (в т.ч. штатная ссылка «Скачать»/открыть в новой вкладке в `MediaLibrary.vue`/`ResultPanel.vue`) исполняет внедрённый JS в origin бэкенда. Это конкретизирует ранее гипотетические пункты №38/№182 («upload не проверяет magic bytes») рабочим эксплойтом. → определять формат по фактическому содержимому (magic bytes/`format_name` из ограниченного allow-list видеоформатов), присваивать расширение по detected-формату, а не по имени от клиента; отдавать `/files` с `Content-Disposition: attachment` и/или безопасным дефолтным `Content-Type` независимо от расширения. `handlers/mod.rs:145-251`

**Статус №202 на 11 июля 2026: ✅ закрыто в раунде 5.** `handlers/upload.rs` публикует только после `ffprobe`, игнорирует client extension и выбирает server suffix по allow-list; `/files` ставит `nosniff` и sandbox CSP.

203. 🟡 `handlers/mod.rs:74-84,300-310` - переход job в `Running` в import/edit-воркерах по-прежнему безусловно перетирает cancel, пришедший в узком окне. Паттерн в обоих воркерах: `if token.is_cancelled() { mark_cancelled(...); return; }`, затем безусловный `update_job(&jid, |j| j.status = Running)`. Отмена, прилетевшая между этими строками (или между захватом permit и этой проверкой), проходит мимо проверки, а безусловный `update_job` возвращает статус в `Running` и реально стартует `download_video`/`run_ffmpeg`; пользователь видит `Running` вместо `Cancelled` до следующего цикла. Это тот же класс гонки, что и №1/№3 (терминальные переходы), но по другому переходу - `596327b` («Make job cancellation atomic») закрыл терминальные переходы и не тронул именно этот. → заменить оба безусловных `update_job(Running)` на `update_job_if_open` и делать ранний `return`, если job уже терминальна (как уже сделано для Done/Cancelled/Error). `handlers/mod.rs:74-84,300-310`

**Статус №203 на 11 июля 2026: ✅ закрыто в раунде 5.** Все переходы `queued`/`running`/validation-error используют `update_job_if_open` и не оживляют terminal job.
204. 🟡 `db.rs:86-135` - `upsert_project` не атомарен: `SELECT`-then-`INSERT`/`UPDATE` без транзакции и без `UNIQUE(video_id)` даёт дублирующиеся project-строки при параллельном автосейве из двух вкладок/окон на одном клипе (проверено эмпирически: 10 параллельных вызовов → 10 строк вместо 1). `get_project_by_video` берёт `ORDER BY updated_at DESC LIMIT 1`, так что восстановление правки не страдает, но `list_projects` показывает дубликаты. → `UNIQUE` на `projects.video_id` + `INSERT ... ON CONFLICT(video_id) DO UPDATE` одним запросом (как уже сделано для `jobs`/`render_cache`), либо `BEGIN IMMEDIATE`-транзакция вокруг `SELECT`+write. `db.rs:86-135`

### Процессы и ffmpeg

205. 🟡 `state.rs:36-37,176-182` - `render_locks` (карта `cache_key → Arc<Mutex<()>>`, добавленная `a550a86` для сериализации одинаковых рендеров) растёт неограниченно: записи никогда не удаляются, а `cache_key` - хэш от всего `EditRequest`, так что почти любое отличие параметров (сдвиг слайдера на 0.001с) даёт новый постоянный ключ на весь срок жизни процесса. → удалять запись после освобождения guard (drop-guard, чистящий ключ при `Arc::strong_count == 1`) либо периодический/LRU-эвикшен. `state.rs:36-37,176-182`
206. 🟡 `tools/mod.rs:339-347` - в ветке нормального/ошибочного завершения (прямой child вышел раньше, чем оба pipe дали EOF - остался фоновый грандчайлд, держащий запись в pipe) шлётся только одиночный `SIGTERM` без эскалации до `SIGKILL` и без подтверждения смерти; в отличие от путей Cancelled/TimedOut (`terminate_child_tree`: TERM → ждать 3с → KILL → ждать 5с), здесь код не ждёт и не эскалирует - грандчайлд, игнорирующий `SIGTERM`, остаётся жить осиротевшим уже после того, как job помечена done/error и permit освобождён. → звать `terminate_child_tree` и в этой ветке при `!out_done || !err_done`, не блокируя основной return (в фоне). `tools/mod.rs:339-347`

### Данные и контракт

207. 🟡 `handlers/projects.rs:33-41` - лимит размера из `3b655c8` покрывает только `video`/`edit`; поле `name` берётся из тела напрямую без проверки, а на `/api/projects` не навешан `DefaultBodyLimit` (в отличие от `/api/upload`), так что единственная граница - дефолтный ~2МиБ лимит тела axum-экстрактора. `POST /api/projects` с `name` длиной почти 2МБ проходит без 413 и пишется в `projects.name` (TEXT без ограничения в схеме). → добавить проверку размера для `name`/`videoId` до вызова `upsert_project`. `handlers/projects.rs:33-41`
208. 🟡 `tools/args.rs:143-152` - `fade_in`/`fade_out` клэмпятся в `normalize_edit_request` к длительности **исходника**, а не к длительности **результата** после trim/segments/speed; `video_filters`/`audio_filters` применяют фейд только при `out_dur > fade`, так что для короткого результата (например trim до 3с при `fade_out=5`, валидном против source_duration=100с) фейд молча выпадает из графа фильтров без ошибки и без сигнала пользователю - легко воспроизводимо на коротких клипах/шортсах. → клэмпить `fade_in`/`fade_out` против `expected_output_secs` (после normalize trim/segments/speed), а не против `source_duration`. `tools/args.rs:143-152`
209. ⚪ `store.ts:500-526,556-559` - `PRESET_KEYS` включает `format`/`codec`/`qualityTier` наравне с чисто визуальными полями; `applyPreset` фильтрует `Object.assign` по `PRESET_KEYS`, но раз экспортные поля туда входят - применение цветового пресета тихо меняет контейнер/кодек/качество экспорта на значения, зафиксированные при сохранении пресета, без предупреждения (обнаруживается только после экспорта). Уточняет и заменяет прежнюю формулировку №48 (см. правку там). → разделить `PRESET_KEYS` на look-группу (цвет/эффекты) и export-группу (format/codec/qualityTier), либо вести export отдельным опциональным флагом «сохранить и настройки экспорта». `store.ts:500-526,556-559`
210. 🟡 `components/RectOverlay.vue:73-89,119-123` - `begin()` вешает `window` `pointermove`/`pointerup`, которые снимаются только внутри `onUp()` - нет `onUnmounted`. Реальный триггер: пользователь тащит ручку кропа (pointer capture активен), одновременно жмёт `Ctrl/Cmd+Z` (глобальный хоткей в `App.vue`, независим от фокуса) - если предыдущий снапшот истории имел `cropEnabled=false`, `applySnapshot` откатывает флаг, `RectOverlay` размонтируется, `root.value` становится `null`, но `window` продолжает слать `pointermove` → следующий `toSrc()` делает `root.value!.getBoundingClientRect()` и бросает `TypeError`. Тот же путь достижим через клавиатурный сброс crop в `EditPanel.vue` при зажатой мышью ручке - обычный однопользовательский сценарий (одна рука на мыши, другая на клавиатуре), не гипотетический multi-input. Обработчики также остаются висеть на `window` бесконечно (утечка). → `onUnmounted(() => { window.removeEventListener(...); dragMode = null })` + заменить `root.value!` на explicit guard в `toSrc()`/`onMove`. `components/RectOverlay.vue:73-89,119-123`
211. ⚪ `handlers/mod.rs:673-717` - `finish_job` в ветке `Err(e)` переводит job в `JobStatus::Error`, но ни разу не вызывает `tracing::error!`/`tracing::warn!` (grep по всему файлу - ноль совпадений на этот паттерн в `finish_job`); упавший рендер/импорт виден только через поллинг клиентом, если тот успел открыть вкладку - в серверных логах не остаётся ни единой диагностической строки. → добавить `tracing::error!("job {jid} failed: {e}")` в `Err`-ветке `finish_job`. `handlers/mod.rs:673-717`
212. 🟡 `.github/workflows/ci.yml:16-19` - CI ставит `ffmpeg`, но не `yt-dlp`; единственный install-шаг - `apt-get install ffmpeg`. Ни один тест в `api.rs`/`render.rs` не запускает реальный `yt-dlp`-процесс (import-тесты используют заглушку `ToolInfo.ytdlp=true`, не настоящий бинарь), так что любая регрессия в построении аргументов `yt-dlp`, парсинге прогресса или флагах вроде `--download-sections` не будет поймана CI. → добавить установку `yt-dlp` в CI и хотя бы один smoke-тест с реальным `download_video` против короткого публичного/локального фикстур-клипа. `.github/workflows/ci.yml:16-19`
213. ⚪ `handlers/mod.rs:358-363` - ошибка `cache_put` после успешного рендера A молча проглатывается; воркер B, дождавшийся того же `cache_key` через `render_locks`, не находит запись в кэше и заново перерендеривает вместо переиспользования готового файла A - не приводит к потере данных (просто двойная работа), но противоречит цели single-flight из `a550a86`. → логировать и/или ретраить `cache_put` перед освобождением render-лока. `handlers/mod.rs:358-363`
214. ⚪ `handlers/mod.rs:620-640` - геометрический клэмп crop/censor может дать нулевую/нечётную ширину-высоту на вырожденном источнике 1px (клэмп к `width/height` из probe не гарантирует чётность после клэмпа на предельно малых размерах). Крайний случай, маловероятный на реальном видео. → дополнительный `max(2, ...)` + чётность после клэмпа. `handlers/mod.rs:620-640`
215. ⚪ `db.rs:137-145,208-228,284-309` - `list_projects`/`load_jobs`/`load_recent_jobs` роняют весь список 500-й ошибкой, если у одной строки испорчен `video_json`/`edit_json`/`result_json` (например от ручного вмешательства в БД или будущей несовместимой миграции) - одна повреждённая строка блокирует доступ ко всем остальным. → пропускать и логировать повреждённую строку вместо падения всего запроса. `db.rs:137-145,208-228,284-309`

### Тесты

216. 🟡 `components/TrimSlider.vue:61-72` - тот же паттерн, что №210: `window` `pointermove`/`pointerup` не снимаются при размонтировании во время активного drag. → тот же фикс, что №210. `components/TrimSlider.vue:61-72`
217. ⚪ `components/RectOverlay.vue:41-53` - `normalizeRect` маскирует `NaN` в `x`/`y` как `0` и в `w`/`h` как минимум вместо явной валидации - повреждённое состояние прямоугольника тихо телепортируется в угол/минимальный размер без сигнала пользователю. → явно валидировать и логировать вместо тихой маскировки. `components/RectOverlay.vue:41-53`
218. ⚪ `Cargo.toml` - нет `rust-toolchain.toml`/зафиксированного MSRV; сборка зависит от плавающего `stable` и в CI (`dtolnay/rust-toolchain@stable`), и в `Dockerfile` (`rust:1-bookworm`) - воспроизводимость между билдами не гарантирована. → закрепить `rust-toolchain.toml` или задокументировать MSRV. `backend/Cargo.toml`

## Раунд 2: перепроверено и подтверждено рабочим (1 июля 2026)

26 раз агенты явно перечитывали код на месте заявленного фикса и подтвердили,
что он корректен (не просто "коммит существует", а "прочитан текущий код,
прослежен сценарий гонки/ошибки, воспроизведения не найдено"): single-flight
рендер-кэша (№29) держит лок через весь рендер+finish+cache_put и корректно
освобождается при ошибке/отмене/панике держателя; убийство process-group реально
достаёт внука `ffmpeg` от `yt-dlp` (№4); post-kill wait на cancel/timeout-путях
ограничен по времени, воркер не виснет, permit не течёт, зомби не остаётся
(фоновый orphan-reaper); `cancel_handler`/`finish_job` не гонятся по терминальным
переходам, новых дедлоков от расширения области локов нет (№1, №3); отмена
`queued`-задачи реально прерывает ожидание permit (№6); `ServeDir` смонтирован
раздельно на `sources/`/`outputs/`, БД и WAL/SHM недостижимы, `..`-traversal
невозможен (№35); CORS - explicit allowlist, fail-closed без `CORS_ALLOW_ORIGINS`,
легитимный dev/nginx origin не сломан (№36); `BIND_ADDR` в Dockerfile
`127.0.0.1`, `docker-compose` расширяет bind только для backend без `ports:`
наружу (безопасно); лимит размера `video`/`edit` в `project_upsert_handler`
измеряет байты корректно и идёт до записи в БД (№39, частично); сегменты
сортируются и пересечения отклоняются (№12); скорость клэмпится централизованно,
рассинхрон видео/аудио при speed>2 не воспроизводится; округление grain до
целого - не баг (тип параметра ffmpeg `noise=alls=` - int 0-100); индексы
`idx_jobs_updated_at`/`idx_render_cache_created_at` есть в схеме (№32);
`library.remove` корректно сериализован через мьютекс на весь read-modify-save
(конкурентный повторный remove безопасен); `validate_url` резолвит DNS и
блокирует известные private/special диапазоны, а раунд 6 закрыл redirect/rebinding через
egress-proxy (№33/№201); ранее задокументированный баг «`RectOverlay` считает координаты
по letterbox-контейнеру, а не по видимому видео» **не воспроизводится** в
текущем коде (`object-fit` нигде не применяется к `.player`) - закрыт как
неактуальный, не как исправленный отдельным коммитом.

## K. Дополнительный архитектурный долг и риски масштабирования

57. CI не ставит `yt-dlp`, поэтому import path покрыт хуже render path.
58. README, Docker и CI могут разойтись по версии Node/Rust/toolchain.
59. Не зафиксирован MSRV/Rust toolchain для backend.
60. `RUST_LOG`/tracing policy не документированы для диагностики.
61. Нет structured job lifecycle logs как отдельного observability contract.
62. Нет metrics endpoint для длительности jobs, ошибок, очереди и cache hit rate.
63. Health endpoint смешивает readiness/liveness и не различает degradation классы.
64. Health не проверяет, что storage и SQLite реально writable.
65. CORS/security policy не управляется конфигом по окружениям.
66. Served media и internal runtime files живут в одном `storage` namespace.
67. Cleanup не имеет dry-run/report mode и явного журнала удалений.
68. Docker Compose не задаёт CPU/memory/pids limits для тяжёлых media процессов.
69. Нет backup/restore инструкции для `storage/app.db` и media файлов.
70. Нет rollback strategy для будущих DB/schema migrations.
71. Нет OpenAPI/schema source of truth в текущем `main`.
72. TypeScript DTO поддерживаются вручную и не генерируются из backend contract.
73. `ProjectDto` объявлен в `api.ts`, а не в едином `types.ts`/generated module.
74. Response DTO не типизированы на backend: много ручного `json!`.
75. Handlers вручную собирают JSON вместо typed response structs.
76. Status codes не нормализованы: async jobs отвечают 200, delete допускает 404 как success.
77. `cancelJob` на frontend игнорирует HTTP статус и тело ответа.
78. `safeFetch` смешивает network failure и backend 5xx.
79. Frontend requests не имеют `AbortController`/timeout на уровне API client.
80. Polling interval фиксирован, без backoff/jitter.
81. Polling не имеет max attempts/deadline на зависшие jobs.
82. Нет idempotency keys для import/edit/upload.
83. Upload sync-flow отличается от job-based import/edit.
84. Нет `GET /api/jobs` для списка jobs и диагностики очереди.
85. Нет request-id/correlation-id в API responses и logs.
86. Error body shape не единый между resources.
87. Plain-text errors всё ещё возможны в projects/upload paths.
88. Library/projects endpoints без pagination/filter/search contract.
89. API не версионирован (`/api/v1` или schema version отсутствуют).
90. README API блок отстаёт от фактических ошибок, cache и jobs details.
91. `EditRequest` совмещает wire DTO, domain input и cache-key normalization.
92. Эффекты представлены россыпью bool/scalar полей вместо typed effect enum.
93. `format`, `codec`, `filter`, `pad`, colors остаются raw strings.
94. Нет `OutputSpec` как отдельной политики формата/кодека/качества.
95. Нет `TimeRange` type с invariant `start < end`.
96. Нет `CropRect` type с invariant внутри source dimensions и even dimensions.
97. Нет `ScaleSpec` type с допустимыми sentinel values `-1/-2`.
98. Speed/volume/fade/fps bounds не централизованы в domain policy.
99. Non-finite/edge numeric inputs не описаны как отдельная validation policy.
100. Расчёт output duration зависит от builder logic, а не от domain plan.
101. Render cache key считается от serialized request, а не от normalized `EditPlan`.
102. Cache key не учитывает ffmpeg version/capabilities/pipeline version.
103. Cache policy не различает source provenance: URL import vs upload.
104. Нет typed `MediaId`/`ProjectId`/`CacheKey`.
105. Project хранит полный video JSON blob вместо нормализованной связи на media.
106. Project edit JSON не имеет versioned schema/migration path.
107. Defaults UI/edit не имеют явной версии для старых проектов.
108. Autosave project не имеет rollback/history.
109. Нет доменного audit trail для изменений проекта.
110. Нет typed domain errors для edit validation.
111. Backend capabilities не оформлены как contract для frontend controls.
112. Preview fidelity не связана с backend compile policy.
113. Source/output media kind не представлен отдельной domain enum везде.
114. Нет corpus-а canonical edit recipes для regression testing.
115. Новое поле edit всё ещё требует правок в нескольких местах.
116. Platform presets живут как UI logic, а не как domain/export presets.
117. Quality tiers не представлены как общая таблица backend/frontend.
118. Нет formal compatibility policy для старых сохранённых проектов.
119. Timeline IR описан в архитектуре, но ещё не является кодовой границей.
120. Domain model не готова к multi-track/range effects без большого переписывания.
121. `backend/src/tools/args.rs` остаётся слишком крупным compile god-file.
122. `backend/src/handlers/mod.rs` всё ещё хранит общую job-машинерию.
123. `backend/src/db.rs` смешивает schema, SQL, DTO mapping и tests.
124. `backend/src/library.rs` остаётся JSON repository рядом с SQLite repository.
125. `lib.rs` строит router с конкретными implementations вместо ports.
126. Нет traits для `ProjectRepo`, `JobRepo`, `MediaRepo`, `RenderCache`.
127. Нет in-memory repositories для быстрых service tests.
128. Нет `AppError`/`IntoResponse` как единого error boundary.
129. Нет `messages.rs`/i18n boundary для пользовательских строк.
130. Process runner и parser progress всё ещё слабо отделены от tool facade.
131. Нет `JobService::spawn` как единственного скелета queued/running/finish.
132. Нет explicit state machine для Job transitions.
133. Cancel tokens живут рядом с app state, а не внутри jobs subsystem.
134. Persist-on-terminal invariant не закреплён типом/service API.
135. Нет тестов на TTL cleanup и active-file protection.
136. Нет тестов на upload concurrency/backpressure.
137. Нет property tests для geometry/timeline normalization.
138. Нет API snapshot tests для JSON response shapes.
139. Нет fuzz/negative corpus для request DTO.
140. Нет benchmark/perf baseline для ffmpeg args builder и large library/projects.
141. Нет component tests для editor panels.
142. Нет browser/E2E smoke на import/open/edit/export UI flow.
143. Нет route/lazy boundary для проектов, медиатеки и будущего audit/ideas surface.
144. Нет error boundary для неожиданных runtime ошибок UI.
145. Toasts не имеют action/retry и не связаны с API error codes.
146. Toast history/debug context не сохраняется.
147. Global keyboard handler живёт в `App.vue`, а не в `useShortcuts`.
148. Shortcut handling не учитывает все a11y/IME/composition случаи.
149. После upload/open/delete нет системного focus management.
150. Drag/drop upload не имеет client-side size/type preflight.
151. URL import warning не связан с backend URL policy.
152. `pollJob` нельзя отменить при unmount/смене клипа. **Уточнено 1 июля 2026: подтверждено, что механизм отсутствует, но impact не воспроизводится - `store.ts` синглтон-модуль, import/export-панели не размонтируются во время активной задачи, поэтому наблюдаемого бага сейчас нет; остаётся debt, не активный дефект.**
153. `setTimeout` polling не хранит handle для cleanup.
154. Import/export status fields дублируют друг друга вместо job view model.
155. Нет UI для persisted jobs history.
156. Media library без search/filter/pagination.
157. Delete media/project без undo/confirm для destructive flow.
158. Result panel и media library не имеют unified download/open actions model.
159. LocalStorage presets без size limit/quota handling UX.
160. Theme хранится локально, но нет first-class system preference mode.
161. Нет reduced-motion/high-contrast accessibility pass.
162. Controls не вынесены в reusable primitives (`Field`, `SegmentedControl`, `Slider`).
163. Options arrays живут в `EditPanel.vue`, а не в `lib/editOptions`.
164. Export hints смешаны с component computed logic.
165. Autosave/project restore side effects не изолированы в composable.
166. Frontend API client не валидирует runtime shape ответов.
167. `window.__store` DEV hook полезен, но не описан как debug-only contract.
168. CSS/design tokens не описаны как система.
169. Нет visual regression/screenshots для responsive editor.
170. Frontend не готов к локализации строк.
171. Нет threat model для локального vs exposed deployment.
172. Нет authentication middleware и token story.
173. Нет CSRF posture для future cookie/session mode.
174. Нет rate limiting на import/upload/edit.
175. Нет per-user isolation для проектов, jobs и files.
176. Нет sandbox profile для ffmpeg/yt-dlp.
177. Нет allowlist/denylist policy для supported URL domains.
178. ✅ Нет redirect policy для yt-dlp после URL validation. **Закрыто в раунде 6:** per-job egress-proxy валидирует каждый target; см. №201.
179. Нет centralized redaction для logs/errors/API.
180. Query tokens в импортируемых URL не редактируются как единая policy.
181. Нет diagnostics bundle с гарантированной redaction.
182. ✅ Upload не проверяет magic bytes до публикации файла через `/files`. **Закрыто в раунде 5:** staging + `ffprobe format_name` + server-selected extension; см. №202.
183. Нет malware/quarantine story для uploaded media.
184. File serving не ставит explicit safe `Content-Disposition`.
185. Нет audit log для mutating operations.
186. Нет quota/usage reporting по `sources`, `outputs`, SQLite и cache.
187. Нет checksum/integrity metadata для source/output files.
188. Нет orphan scanner/repair tool для storage/library/db/cache.
189. Нет backup/export/import command для проектов и медиатеки.
190. Нет DB maintenance story: vacuum/analyze/checkpoint.
191. Нет transaction boundary для file write + DB/cache/library update.
192. Нет media table с FK на projects/jobs/render_cache.
193. Cleanup не считает reference counts для files.
194. Disk-full ошибки не превращаются в понятный user-facing state.
195. Startup не проверяет permissions/free space заранее.
196. Нет filesystem lock для защиты от двух backend процессов на одном storage.
197. Нет retention policy для проектов.
198. Нет retention policy для старых source files отдельно от outputs.
199. Нет runbook для ручного восстановления после broken DB/storage drift.
200. Нет регулярного docs/code drift check, который гарантирует актуальность этой двухсотки.

---

## Проверено и опровергнуто (14 ложных срабатываний)

Верификация отсекла находки, которые при чтении кода не подтвердились - полезно,
чтобы не тратить время:

- **Инъекция команд** (`tools/mod.rs:83-103`) - SAFE: `Command` без shell, всё через argv; `sanitize_color`→`black`, `filter_preset` - фикс-таблица, числа форматируются.
- **Path-traversal через `video_id`** (`tools/mod.rs:163-186`) - SAFE: скан директории по равенству stem, без `Path::join` от ввода.
- **`library.json` переписывается целиком** - НЕ баг: запись атомарна (tmp+rename), осознанный trade-off для маленькой однопользовательской библиотеки.
- **Пустой ключ `render_cache` при ошибке сериализации** - НЕ баг: `serde_json` пишет `null` для не-finite f64 (не ошибка), а NaN вообще не десериализуется из тела.
- **Ключ кэша игнорирует env/расширение** - НЕ баг: формат входит в сериализованный `EditRequest`, расширение определяется форматом, на хите есть проверка существования файла.
- **`set_job` создаёт окно 404 после ответа** - НЕ баг: и persist, и in-memory вставка завершаются до ответа; остаётся лишь мелочь - проглоченная ошибка persist (см. №31).
- **`VideoPreview` `playerTime` фанит через два deep-watch** - НЕ баг: `playerTime` - top-level поле, вне `state.edit`/`state.video`, watch не срабатывает.
- **Гонка автосейва с переоткрытием клипа** - НЕ баг: `persistProject` читает текущий `state.video.id`+`state.edit`, watch перепланирует таймер при смене клипа, `restoreProject` гейтит по id.
- **Quality-tier молча теряется для prores/gif/png/jpg/mp3** - НЕ баг: чипы качества не показываются для этих форматов (`showQuality`), CRF для них бессмыслен.
- **Quality-tier av1 mismatch** - НЕ баг: то же, гейтится `showQuality`; `tierToCrf` намеренно `null` вне списка.
- **`poll_terminal` флейки на current-thread runtime** - НЕ баг: кооперативное планирование Tokio, тестируемые пути не блокируют.
- **Тест `recover_jobs running→interrupted`** - НЕ баг: транзишн реально проверяется через `recover_jobs` + HTTP-роут (формулировка находки неточна).
- **Concat дублирует всю сборку фильтров/кодеков** - переоценено: тяжёлая логика уже вынесена в общие `video_filters`/`audio_filters`/`push_video_codec`; остаётся тривиальный дубль.
- **Безлимитный progress-канал = DoS** - переоценено: ffmpeg шлёт прогресс ~1/с, drain без `await` под локом; в худшем случае - стилистический нит (`watch::channel` был бы идиоматичнее).

**Раунд 2 (1 июля 2026), ещё 2 опровергнуты:**

- **«Нет auth/ownership на `project_get`/`project_list`/`project_delete` - новая проблема из-за `3b655c8`»** - НЕ новая: `3b655c8` затронул только `project_upsert_handler`, три перечисленных хендлера коммит не трогал вообще; сам факт отсутствия auth/ownership подтверждён чтением кода, но это тот же самый пункт №34/№172, известный и отслеживаемый до этого коммита, не регрессия и не новая находка.
- **«`loadPresets`/`job.result` приводятся через `as` без runtime-валидации → чужой JSON тихо портит state»** - central-claim факт-чек не прошёл: `job.result` приходит с **собственного** бэкенда (`GET /api/jobs/:id`), а не «чужой JSON», и обрабатывается как любой другой typed API-ответ в приложении; ошибочная форма от бэкенда даёт serde-reject → HTTP 400, а не тихую порчу. Узкая часть про `loadPresets` (localStorage без schema-валидации) технически верна, но прослеженный impact - максимум один отклонённый 400-запрос или no-op merge отсутствующего поля, не «затирает геометрию» - понижена и переформулирована в исходном пункте №48.

---

Порядок устранения - в [recommendation.md](../recommendation.md) (P0 → P1 → P2).
Полный список подтверждённых (118) и их `file:line` - в результатах прогона;
здесь сведён топ-200 по влиянию: 56 verified-first + 144 архитектурных долга.
Раунд 2 (1 июля 2026) добавил 18 новых пронумерованных пунктов (201-218),
переформулировал №48 и понизил №45/№152 по итогам adversarial-проверки текущего
кода - см. «Топ-50 актуальных проблем» в начале файла и разделы «Раунд 2» выше.
