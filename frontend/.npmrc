# This project uses only public npm packages. Override a machine-level private
# registry so Svelte, Vite and the test toolchain install reproducibly.
registry=https://registry.npmjs.org/
@sveltejs:registry=https://registry.npmjs.org/
