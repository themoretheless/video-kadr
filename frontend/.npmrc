# This project uses the public npm registry. The machine's global ~/.npmrc points
# npm at GitHub Packages, which does not host vue/vite, so override it locally.
registry=https://registry.npmjs.org/
@vitejs:registry=https://registry.npmjs.org/
